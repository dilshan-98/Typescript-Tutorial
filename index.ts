// 1. How to compile Typescript to Javascript using Typescript Compiler 

/*let id: number = 5;

id = "5";    ----> This will output an error because "id" is a number variable.

To continuously watch the changes to the file in TS, use command "tsc.cmd --watch .\index.ts". This will update when--
--changes are done to the file.*/



// 2. Setup tsconfig file

//command "tsc.cmd --init" will generate the "tsconfig.json" file.